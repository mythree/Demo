************Connection Service******************
This simple application, where two rail roads are connected with  will accepts two parameters, Origin and destination. Each time, if we run the URL, if the selected Origin and destination is with in the list of city.txt file. If will pop-up with “YES” else “No”. I have written the test Junit test Condition for pass and fail scenario.   